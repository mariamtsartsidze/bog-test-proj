import { css } from "lit-element";

export const genericButtonStyles = css`
  .btn-generic {
    border: none;
    font-weight: bold;
    outline: none;
    cursor: pointer;
  }

  .btn-generic:active {
    transform: translateY(3px);
  }
`;
